PORT=8787
# Required: clients must send this as a Bearer token.
OMNIGATE_API_KEY=
OMNIGATE_DB_PATH=.data/omnigate.sqlite

# Provider API keys — names must match the api_key_env values in src/config/provider.registry.yaml.
# Replace these placeholders with the credentials for the providers you wire up in the registry.
PROVIDER_A_API_KEY=
PROVIDER_B_API_KEY=
PROVIDER_C_API_KEY=
PROVIDER_D_API_KEY=
