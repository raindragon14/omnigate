{
  "name": "omnigate",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun run --watch src/server.ts",
    "test": "bun test src/ && bun test tests/",
    "test:feature": "bun test src/",
    "test:integration": "bun test tests/",
    "typecheck": "bun x tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "yaml": "^2.6.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.7.2"
  }
}
